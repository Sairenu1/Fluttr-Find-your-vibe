# Server Configuration
server.port=8080
spring.application.name=SparkMate

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/sparkmate
spring.datasource.username=postgres
spring.datasource.password=sairenu
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=mySecretKeyForSparkMateApp2024ThisShouldBeLongAndSecureAtLeast256BitsLongForProduction
jwt.expiration=86400000

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# Logging
logging.level.com.sparkmate=INFO
logging.level.org.springframework.security=INFO